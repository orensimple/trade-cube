replicaCount: 1

image:
  repository: orensimple/trade-core-app
  tag: latest

service:
  type: NodePort
  port: 80

metrics:
  enabled: false
  serviceMonitor:
    enabled: false
  
ingress:
  enabled: true
  hosts:
    - host: arch.homework
      paths: ["/"]
  metrics:
    enabled: false
    serviceMonitor:
      enabled: false
